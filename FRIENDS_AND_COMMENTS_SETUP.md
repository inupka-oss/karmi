# 🎉 Друзья, Watch Party и Комментарии - Настройка

## ✅ Что добавлено:

### 1. Система друзей
- ✅ Добавление друзей по email
- ✅ Заявки в друзья (принять/отклонить)
- ✅ Список друзей в профиле
- ✅ Уведомления о приглашениях

### 2. Watch Party с друзьями
- ✅ Кнопка "👥 Друзья" в Watch Party
- ✅ Быстрое приглашение из списка друзей
- ✅ Уведомления о приглашении на просмотр

### 3. Исправленные комментарии
- ✅ Лучшая обработка ошибок
- ✅ Показ конкретных ошибок
- ✅ Исправлена отправка комментариев

---

## 📋 Шаг 1: SQL Миграция (ОБЯЗАТЕЛЬНО!)

Выполните в **Supabase SQL Editor**:

```sql
-- Скопируйте всё из файла:
-- supabase/migrations/002_friends_and_fixes.sql
```

Или перейдите по пути: `supabase/migrations/002_friends_and_fixes.sql`

**Что создаётся:**
- Таблица `user_friends` - заявки и друзья
- View `user_friends_with_profiles` - друзья с никами
- View `user_friends_stats` - статистика друзей
- Функции: `send_friend_request`, `accept_friend_request`, `send_watch_party_invite`
- Таблица `notifications` - уведомления
- Исправления для `comments`

---

## 📋 Шаг 2: Включить Realtime

В **Supabase Dashboard**:

```
Database → Replication → Включить для таблиц:
✅ user_friends
✅ notifications
✅ watch_parties
✅ comments
```

---

## 📋 Шаг 3: Проверка RLS Policies

Убедитесь что RLS включён для всех таблиц:

```sql
-- Проверка
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public';
```

Все таблицы должны иметь `rowsecurity = true`.

---

## 🎮 Как использовать

### Добавить друга:

1. Откройте **Профиль** (`/profile`)
2. Найдите блок "**👥 Друзья**"
3. Нажмите "**➕ Добавить**"
4. Введите **email друга** (который он использовал при регистрации)
5. Нажмите "**Отправить**"

### Принять заявку в друзья:

1. Откройте **Профиль**
2. В разделе "**📨 Входящие заявки**" увидите друга
3. Нажмите "**✓ Принять**"

### Пригласить в Watch Party:

1. Откройте страницу **аниме**
2. Нажмите "**🎉 Смотреть с друзьями**"
3. Введите имя и создайте комнату
4. Нажмите кнопку "**👥 Друзья**" (розовая)
5. Нажмите "**Пригласить**" напротив друга
6. Друг получит уведомление!

---

## 🔔 Уведомления

Друзья получают уведомления:
- 📨 **Заявка в друзья** - когда вы отправляете запрос
- 🎉 **Приглашение на просмотр** - когда приглашаете в комнату

Уведомления сохраняются в таблице `notifications`.

---

## 🐛 Если комментарии не работают:

### 1. Проверьте таблицу comments:

```sql
SELECT * FROM comments LIMIT 5;
```

### 2. Проверьте RLS policies:

```sql
-- Должно разрешать SELECT всем
SELECT * FROM pg_policies WHERE tablename = 'comments';
```

### 3. Включите логи:

В консоли браузера (F12) смотрите ошибки при отправке комментария.

---

## 📁 Новые файлы:

| Файл | Описание |
|------|----------|
| `supabase/migrations/002_friends_and_fixes.sql` | SQL миграция (друзья + уведомления) |
| `src/components/FriendsList.tsx` | Компонент списка друзей |
| `src/components/WatchParty.tsx` | Обновлён с кнопкой друзей |
| `src/app/profile/page.tsx` | Добавлен FriendsList |
| `src/components/CommentSection.tsx` | Исправлена отправка |

---

## 🎊 Готово!

Теперь у вас есть:
- ✅ Система друзей
- ✅ Быстрые приглашения в Watch Party
- ✅ Работающие комментарии

**Запустите `npm run dev` и тестируйте!** 🚀
